apiVersion: v1
kind: Service
metadata:
  name: {{ include "openl_ws.fullname" . }}
  labels:
    {{- include "openl_ws.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openl_ws.selectorLabels" . | nindent 4 }}
