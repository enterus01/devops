apiVersion: v1
kind: Service
metadata:
  name: {{ include "openl_webstudio.fullname" . }}
  labels:
    {{- include "openl_webstudio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openl_webstudio.selectorLabels" . | nindent 4 }}
